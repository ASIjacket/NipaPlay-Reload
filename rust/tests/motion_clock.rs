// Can also run with rustc --test, without building the application's GPU/media
// dependencies. This exercises the exact clock and sampling code used at runtime.
#[path = "../src/next2_engine/engine/motion.rs"]
mod motion;
